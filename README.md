# Feature-Study-
This is a code that will take a dataset as input and generate a feature summary table (such as number of missing values, value types etc.). This is very useful in the data pre-processing stage in order to make decisions such as which features to include and remove during analysis! 
